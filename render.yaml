services:
  - type: web
    name: omnilink-frontend
    env: static
    buildCommand: ./render-build.sh
    staticPublishPath: omnilink_frontend/build/web
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
